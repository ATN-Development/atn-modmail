export default {
  example:
    "Hello World!" /** Use the same syntax as the example to actually add a snippet, 
  before : goes the name of the snippet, after the content of it. 
  Note that if you need to write " remember to type a \ before it!*/,
};
